program SPMailSpam;

uses
  ShareMem,
  Forms,
  MailSpamMainFrm in 'MailSpamMainFrm.pas' {MainForm};

{$R *.RES}

begin
  Application.Initialize;
  Application.Title := 'FIVE Mailer';
  Application.CreateForm(TMainForm, MainForm);
  Application.Run;
end.
